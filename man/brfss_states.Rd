% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{brfss_states}
\alias{brfss_states}
\title{BRFSS reporting jurisdictions: FIPS codes, names, and Census regions}
\format{
A tibble with 56 rows and 5 columns:
\describe{
\item{fips}{Census state FIPS code (integer), as in \verb{_STATE}.}
\item{name}{Jurisdiction name, e.g. \code{"Texas"}.}
\item{abbr}{Two-letter postal abbreviation, e.g. \code{"TX"}.}
\item{region}{Census region (\code{Northeast}, \code{Midwest}, \code{South},
\code{West}); \code{NA} for territories, which the Census regions do not
cover.}
\item{division}{Census division, e.g. \code{"West South Central"};
\code{NA} for territories.}
}
}
\source{
Census state FIPS codes (FIPS PUB 5-2) and Census regions
and divisions; jurisdiction list cross-checked against CDC's
\verb{_STATE} format maps. Not every jurisdiction participates every
year; see the \code{datasets} article for how reporting areas changed.
}
\usage{
brfss_states
}
\description{
Every jurisdiction that appears in the BRFSS \verb{_STATE} value-label
maps: the 50 states, the District of Columbia, and the participating
territories (American Samoa, Guam, Palau, Puerto Rico, Virgin
Islands). \code{fips} matches the \verb{_STATE} column in the data, so this
table joins directly onto any extract, and it is what the \code{states}
argument of \code{\link[=read_brfss]{read_brfss()}} and \code{\link[=brfss_design]{brfss_design()}} accepts names and
postal abbreviations from.
}
\examples{
brfss_states
}
\seealso{
The \code{states} argument of \code{\link[=read_brfss]{read_brfss()}} and
\code{\link[=brfss_design]{brfss_design()}}; the \emph{Merging BRFSS with external data} article.
}
\keyword{datasets}
