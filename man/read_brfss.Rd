% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_brfss.R
\name{read_brfss}
\alias{read_brfss}
\title{Read BRFSS survey microdata}
\usage{
read_brfss(
  years,
  vars = NULL,
  states = NULL,
  download = TRUE,
  quiet = FALSE,
  labels = FALSE,
  na = FALSE
)
}
\arguments{
\item{years}{Integer vector of survey years, e.g. \code{2023} or
\code{2019:2023}. See \code{\link[=brfss_years]{brfss_years()}} for what is available.}

\item{vars}{Optional character vector of variable names to return.
The default returns every variable. Names are matched
case-insensitively (\code{"genhlth"} finds \code{GENHLTH}), and returned
columns always carry CDC's canonical spelling. Use \code{\link[=brfss_vars]{brfss_vars()}}
to search names across years.}

\item{states}{Optional vector restricting rows to those reporting
jurisdictions: state FIPS codes, postal abbreviations, or names,
mixed freely and matched case-insensitively (\code{c(48, "CA", "maine")}). See \link{brfss_states} for the full list. The filter is
pushed into the DuckDB query, so other states' rows never reach R,
and the \verb{_STATE} column is always returned so the filter stays
visible. A requested state absent from a requested year's file
(states do occasionally miss a year) raises a classed warning
rather than returning silently fewer rows.}

\item{download}{If \code{FALSE}, only cached years are used and missing
years raise an error instead of being downloaded.}

\item{quiet}{If \code{TRUE}, suppress download progress output.}

\item{labels}{Controls value-label conversion via CDC's format
libraries (available from 1998 on). \code{FALSE} (the default) keeps
every numeric code. \code{TRUE} converts variables with safe maps to
factors; note the conversion is lossy: the CDC codes are gone, and
\code{as.numeric()} on the result returns factor level positions, not
codes (most CDC code sets are non-contiguous, so the two disagree).
\code{"both"} keeps the code in the level text (\code{"[1] Excellent"}) so it
stays recoverable. A variable converts only when its format is a
pure code-to-label map, its code set agrees across the requested
years, every observed value is covered, and its label wording did
not change meaning across those years; everything else keeps its
numeric codes. Wording that did change (CDC reused \code{COLNTES1}
codes 3 to 5 for different screening intervals from 2022 on) keeps
its codes too, with a \code{brfssdata_label_drift_warning} naming the
variables; read those years separately if you want each year's own
wording. Levels come from the newest requested year, so purely
cosmetic rewording is shown in CDC's most recent phrasing.
Identifier and design columns (\verb{_STATE}, the
weights, strata, and PSU) always keep numeric codes so filters like
\verb{_STATE == 6} keep working. See \code{\link[=brfss_labels]{brfss_labels()}} for the catalog.}

\item{na}{If \code{TRUE}, set the codes CDC uses for missing-type answers
(don't know / not sure, refused, not asked) to \code{NA}, using the
value-label catalog; see \code{\link[=brfss_missing_codes]{brfss_missing_codes()}} for exactly which
codes. The default here is \code{FALSE}: \code{read_brfss()} returns the file
as CDC published it. (\code{\link[=brfss_design]{brfss_design()}} defaults to \code{TRUE}, because
estimates over raw codes are almost never what an analyst wants.)
Code 88/888 ("None") means zero, is never touched, and needs
recoding to 0 by hand before averaging count variables. Labels
cover 1998 on, so earlier years pass through unchanged, and a
request touching years the catalog does not cover (or, like 1998,
covers only partially) says so with a
\code{brfssdata_na_coverage_note} message rather than staying silent.}
}
\value{
A tibble with one row per respondent and a \code{year} column.
}
\description{
Returns respondent-level BRFSS data for one or more survey years as a
tibble. Each requested year is downloaded once into the local cache
(see \code{\link[=brfss_cache_dir]{brfss_cache_dir()}}) and read from there afterwards; the query
itself runs through DuckDB, so selecting a handful of variables from a
300-plus column survey stays fast.

Different survey years carry different variable sets. When years are
combined, variables absent from a year are filled with \code{NA}. A \code{year}
column always identifies the survey year of each row.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# General health and design variables for two years
dat <- read_brfss(2022:2023, vars = c("GENHLTH", "_LLCPWT"))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=brfss_design]{brfss_design()}} to get a survey-design object instead of a
plain tibble; \link{brfssdata-conditions} for the classes of every error,
warning, and message this package signals.
}
