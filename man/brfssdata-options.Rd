% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brfssdata-options.R
\name{brfssdata-options}
\alias{brfssdata-options}
\title{Options that change how brfssdata behaves}
\description{
Every session option the package reads, in one place. Set any of
them with \code{\link[=options]{options()}}, typically in a project \code{.Rprofile}; none is
required for normal use.
}
\section{Options}{

\describe{
\item{\code{brfssdata.cache_dir}}{Path used for the local data cache
instead of the \code{\link[tools:R_user_dir]{tools::R_user_dir()}} default. Point it at a
shared or project-local directory to reuse one set of downloads
across machines or projects; see \code{\link[=brfss_cache_dir]{brfss_cache_dir()}} and the
\emph{Getting started} vignette's offline recipe.}
\item{\code{brfssdata.lonely_psu}}{A single string copied into
\code{options(survey.lonely.psu = ...)} when \code{\link[=brfss_design]{brfss_design()}} detects
single-PSU strata, replacing the package's default \code{"adjust"}.
See the \emph{Survey design} article for what the settings mean.}
\item{\code{brfssdata.module_weight_check}}{Set to \code{FALSE} to disable
the optional-module weight diagnostic that \code{\link[=brfss_design]{brfss_design()}} runs
when \code{vars} is supplied (the
\code{brfssdata_module_weight_warning} signal).}
\item{\code{brfssdata.repo}}{Advanced. The GitHub repository
(\code{"owner/name"}) whose releases host the data, for forks that
publish their own builds or air-gapped mirrors. Checksums still
come from that repository's manifest, so pointing here at a repo
you do not trust extends your trust to its data. Not needed for
normal use.}
}

The package also \emph{writes} one option while working:
\code{\link[=brfss_design]{brfss_design()}} sets \code{options(survey.lonely.psu)} for the session
(announced once via \code{brfssdata_lonely_psu_note}), and downloads
temporarily raise \code{options(timeout)} to at least an hour.
}

\seealso{
\link{brfssdata-conditions} for the condition classes that
control console output, \code{\link[=brfss_cache_dir]{brfss_cache_dir()}} for cache management.
}
