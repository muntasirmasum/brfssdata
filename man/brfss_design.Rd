% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brfss_design.R
\name{brfss_design}
\alias{brfss_design}
\title{Build a survey-design object for BRFSS analysis}
\usage{
brfss_design(
  years,
  vars = NULL,
  allow_break = FALSE,
  pool_weights = TRUE,
  download = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{years}{Integer vector of survey years, e.g. \code{2023} or
\code{2019:2023}. See \code{\link[=brfss_years]{brfss_years()}} for what is available.}

\item{vars}{Optional character vector of analysis variables to carry
into the design. Design variables are always included.}

\item{allow_break}{Set to \code{TRUE} to permit pooling years across the
2011 methodology change. A warning is still issued.}

\item{pool_weights}{If \code{TRUE} and more than one year is requested,
divide each weight by the number of years.}

\item{download}{If \code{FALSE}, only cached years are used and missing
years raise an error instead of being downloaded.}

\item{quiet}{If \code{TRUE}, suppress download progress output.}
}
\value{
A \code{tbl_svy} survey-design object. The underlying data carry
three added syntactic columns the design is built on: \code{brfss_wt}
(the selected, possibly pooled, weight), \code{brfss_psu}, and
\code{brfss_strata} (the raw stratum for a single year; the
year-by-stratum interaction when years are pooled). The original
CDC columns are kept unchanged.
}
\description{
Returns a \code{\link[srvyr:as_survey_design]{srvyr::as_survey_design()}} \code{tbl_svy} with the complex sampling
design applied: primary sampling units (\verb{_PSU}), strata (\verb{_STSTR}), and
the year-appropriate final weight. Weight selection is automatic:
\verb{_FINALWT} for years before 2011 (post-stratification era) and
\verb{_LLCPWT} from 2011 on (raking era).

CDC states that estimates from 2011 onward are not directly comparable
to earlier years, because 2011 added cell-phone-only respondents and
replaced post-stratification with raking. Requests that pool years from
both sides of that boundary therefore fail unless \code{allow_break = TRUE}
is set deliberately.

When several years are combined, weights are divided by the number of
years (\code{pool_weights = TRUE}, the default) so that pooled estimates
represent an average year rather than a sum of populations, and the
variance strata become the year-by-stratum interaction, treating each
annual survey as an independent sample.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
library(srvyr)
des <- brfss_design(2023, vars = "GENHLTH")
des |>
  group_by(GENHLTH) |>
  summarize(prop = survey_prop())
\dontshow{\}) # examplesIf}
}
