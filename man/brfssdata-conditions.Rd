% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{brfssdata-conditions}
\alias{brfssdata-conditions}
\title{Conditions signaled by brfssdata}
\description{
Every error, warning, and message this package signals carries a
class, so \code{tryCatch()} and \code{withCallingHandlers()} can react to
exactly the situation they mean to and nothing else, e.g.
\verb{tryCatch(read_brfss(2023), brfssdata_download_error = \\(e) NULL)} or
\code{suppressWarnings(..., classes = "brfssdata_break_warning")}.

\code{quiet = TRUE} never hides a signal about what the data mean; it
suppresses progress and housekeeping output only. To silence a
specific analytical note, suppress its class, e.g.
\code{suppressMessages(read_brfss(2021:2022, vars = "_DRNKWK1"), classes = "brfssdata_rename_note")}.
}
\section{Errors}{

\describe{
\item{\code{brfssdata_bad_years_arg}}{\code{years} is not a vector of whole
survey years.}
\item{\code{brfssdata_bad_year}}{A requested year is not among the
published releases.}
\item{\code{brfssdata_no_data}}{The data manifest could not be read or
lists no published years.}
\item{\code{brfssdata_bad_vars_arg}}{\code{vars} is not a character vector
of variable names.}
\item{\code{brfssdata_bad_var}}{A requested variable does not exist in
the requested years.}
\item{\code{brfssdata_bad_states_arg}}{\code{states} is not a vector of
FIPS codes, postal abbreviations, or jurisdiction names.}
\item{\code{brfssdata_bad_state}}{A value in \code{states} matches no BRFSS
jurisdiction; see \link{brfss_states}.}
\item{\code{brfssdata_bad_pattern}}{\code{pattern} in \code{\link[=brfss_vars]{brfss_vars()}} is not a
valid regular expression.}
\item{\code{brfssdata_bad_weight}}{\code{weight} in \code{\link[=brfss_design]{brfss_design()}} is
malformed, requested outside the weight's published span, absent
from a requested year, or carries values that are not positive
and finite.}
\item{\code{brfssdata_unrecognized_weight}}{\code{weight} in \code{\link[=brfss_design]{brfss_design()}}
names a column that is not one of CDC's final analysis weights
and \code{unsafe_weight = TRUE} was not set. Also carries
\code{brfssdata_bad_weight}, so one handler catches every weight
refusal.}
\item{\code{brfssdata_bad_bool_arg}}{A \code{TRUE}/\code{FALSE} argument received
something else, \code{NA} included. Each flag also raises a class of
its own on one pattern, \verb{brfssdata_bad_<argument>_arg}: \code{na}
raises \code{brfssdata_bad_na_arg}, and likewise for \code{download},
\code{quiet}, \code{refresh}, \code{verify}, \code{catalogs}, \code{allow_break},
\code{pool_weights}, and \code{unsafe_weight}. One handler on the shared
class catches them all.}
\item{\code{brfssdata_bad_labels_arg}}{\code{labels} is something other than
\code{TRUE}, \code{FALSE}, or \code{"both"}.}
\item{\code{brfssdata_bad_n_arg}}{\code{n} in \code{print.brfss_codebook()} is not
a single positive number.}
\item{\code{brfssdata_bad_option}}{\code{options(brfssdata.lonely_psu)} is
not a single string, \code{options(brfssdata.module_weight_check)}
is not \code{TRUE} or \code{FALSE}, or \code{options(brfssdata.cache_dir)} is
not a single non-empty path to a directory.}
\item{\code{brfssdata_bad_design_var}}{A design variable (era weight,
\verb{_STSTR}, \verb{_PSU}) is absent or carries missing or invalid
values, so no valid design can be built; for a final analysis
weight this points at a damaged file.}
\item{\code{brfssdata_no_eligible_rows}}{No rows are left to build a
survey design: a \code{states} filter, or the domain of a
user-supplied \code{weight}, emptied the frame. \code{\link[=read_brfss]{read_brfss()}} still
returns the zero-row tibble, which is a usable answer; a
zero-row survey design is not constructible.}
\item{\code{brfssdata_break_error}}{The requested years span the 2011
redesign and \code{allow_break = TRUE} was not set.}
\item{\code{brfssdata_not_cached}}{\code{download = FALSE} was set and the
needed file is not in the cache.}
\item{\code{brfssdata_download_error}}{A download failed. Also the
parent class of \code{brfssdata_checksum_error}, so one handler
catches both.}
\item{\code{brfssdata_checksum_error}}{A downloaded file did not match
the manifest's sha256 after a retry; nothing was cached.}
\item{\code{brfssdata_cache_unwritable}}{The cache directory could not
be created, or exists but cannot be written to, so no download
can land: a local permission problem, named as one instead of
being reported as a network failure. Also carries
\code{brfssdata_download_error}, so the metadata lookups' bundled
fallback still applies.}
\item{\code{brfssdata_corrupt_cache}}{A cached file is unreadable
(typically a corrupted download from before verification); the
message names the file and the \code{\link[=brfss_cache_clear]{brfss_cache_clear()}} remedy.}
\item{\code{brfssdata_wrong_year_cache}}{A cached file does not hold
the survey year its name promises (a hand-copied or damaged
cache); the message says what each such file really holds. Also
carries \code{brfssdata_corrupt_cache}, so one handler covers both.}
\item{\code{brfssdata_duckdb_version}}{The installed duckdb is older
than the version this package requires, so the argument that
keeps DuckDB from writing to the home directory is unavailable;
the message names the required and the found version.}
\item{\code{brfssdata_type_conflict}}{A requested column is stored as
text in some requested years' files and as a number in others,
so combining them would silently corrupt values; usually stale
cached files mixed with current releases, with the
\code{\link[=brfss_cache_clear]{brfss_cache_clear()}} remedy named.}
}
}

\section{Warnings}{

\describe{
\item{\code{brfssdata_break_warning}}{Pooling across the 2011 redesign
with \code{allow_break = TRUE}.}
\item{\code{brfssdata_intermediate_weight_warning}}{\code{weight} in
\code{\link[=brfss_design]{brfss_design()}} names an intermediate stage of CDC's weighting
pipeline (e.g. \verb{_LLCPWT2}, the truncated pre-raking design
weight), requested deliberately via \code{unsafe_weight = TRUE}.}
\item{\code{brfssdata_unsafe_weight_warning}}{\code{weight} in
\code{\link[=brfss_design]{brfss_design()}}, requested via \code{unsafe_weight = TRUE}, names a
column that is neither a final analysis weight nor a known
pipeline stage; the estimates are calibrated to nothing.}
\item{\code{brfssdata_module_weight_warning}}{A requested analysis
variable has data almost only where a module weight
(\verb{_CLLCPWT} and kin) is non-missing, but the design uses a
full-sample weight: very likely a module analysis under the
wrong weight. State-optional modules that CDC assigns to the
core weight are the legitimate exception. Disable with
\code{options(brfssdata.module_weight_check = FALSE)}.}
\item{\code{brfssdata_pooled_states_warning}}{Pooled years differ in
state participation, so totals mix coverage. Participation is
counted over the rows a user-supplied \code{weight} covers, the
population the design actually estimates, not over the whole
file.}
\item{\code{brfssdata_empty_year_warning}}{A requested year contributed
no rows to a pooled design (a \code{states} filter, or the domain of
a user-supplied \code{weight}, emptied it), so pooled weights divide
by the contributing years only and totals estimate an average
contributing year.}
\item{\code{brfssdata_label_drift_warning}}{Label wording for a
variable changed meaning (not just formatting) across the
requested years, so it kept CDC's numeric codes instead of
converting to a factor; read the years separately if each
year's own wording is wanted.}
\item{\code{brfssdata_na_coverage_warning}}{\code{na = TRUE} recoded nothing
in a requested year, either because no value-label catalog
exists for it (years before 1998) or because the catalog covers
none of the loaded variables there: estimates over that year
still contain CDC's don't-know and refused codes.}
\item{\code{brfssdata_state_coverage_warning}}{A jurisdiction requested
via \code{states} is absent from a requested year's file, so
estimates for that year cover the remaining states only.}
}
}

\section{Messages}{

\describe{
\item{\code{brfssdata_cache_note}}{Cache lifecycle notes: directory
created, files removed by \code{\link[=brfss_cache_clear]{brfss_cache_clear()}}, a
size-mismatched or checksum-failing cached file re-downloaded,
a stale catalog refreshed, or the \code{\link[=brfss_download]{brfss_download()}} summary.}
\item{\code{brfssdata_download_note}}{A survey year is being downloaded
(once, then cached).}
\item{\code{brfssdata_manifest_note}}{The manifest or a catalog could
not be refreshed; a cached or bundled copy was used.}
\item{\code{brfssdata_lonely_psu_note}}{The once-per-session note that
\code{survey.lonely.psu} was set to \code{"adjust"}.}
\item{\code{brfssdata_design_spec_note}}{The specification of the
design just built, stated the way a Stata log would (weight,
strata, PSU term, pooling divisor; one \code{svyset} line per era
weight when pooling crosses 2011), for cross-checking against a
coauthor's \code{svyset}. Suppressed by \code{quiet = TRUE}.}
\item{\code{brfssdata_unverified_note}}{An asset was downloaded without
checksum verification (the available manifest carries no hash
for it).}
\item{\code{brfssdata_na_note}}{\code{na = TRUE} set missing-type codes to
\code{NA}; the counts and the \code{\link[=brfss_missing_codes]{brfss_missing_codes()}} audit trail.}
\item{\code{brfssdata_na_coverage_note}}{\code{na = TRUE} was requested for
a year the value-label catalog covers only partially (1998
covers under a quarter of its file's variables), so codes in the
uncatalogued variables passed through unchanged. Years with no
catalog at all raise \code{brfssdata_na_coverage_warning} instead.}
\item{\code{brfssdata_weight_subset_note}}{A user-supplied \code{weight} in
\code{\link[=brfss_design]{brfss_design()}} is missing on some rows (a module weight covers
only its module's records); those rows were dropped, per CDC's
module-analysis guidance.}
\item{\code{brfssdata_empty_result}}{A metadata lookup
(\code{\link[=brfss_vars]{brfss_vars()}}, \code{\link[=brfss_labels]{brfss_labels()}}, \code{\link[=brfss_crosswalk]{brfss_crosswalk()}},
\code{\link[=brfss_year_info]{brfss_year_info()}}) matched nothing. From \code{\link[=brfss_vars]{brfss_vars()}} the
message also suggests near misses: close names and labels,
order-blind multi-word matches, and matches confined to other
years.}
\item{\code{brfssdata_partial_match_note}}{Some requested variables in
\code{\link[=brfss_labels]{brfss_labels()}} or \code{\link[=brfss_crosswalk]{brfss_crosswalk()}} matched nothing while
others matched, so the returned rows cover the matching
variables only. Absence can be legitimate: continuous variables
have no label entries, and most variables belong to no rename
family.}
\item{\code{brfssdata_full_load_note}}{\code{\link[=read_brfss]{read_brfss()}} is loading every
column because \code{vars} was not given; \code{\link[=brfss_design]{brfss_design()}} passes
\code{vars} through and inherits it.}
\item{\code{brfssdata_rename_note}}{A requested variable is empty in
years a sibling generation from the rename crosswalk covers;
see \code{\link[=brfss_crosswalk]{brfss_crosswalk()}}.}
\item{\code{brfssdata_case_match_note}}{\code{vars} matched columns
case-insensitively; the note pairs each requested spelling with
the CDC-canonical column name the returned data actually use.
Suppressed by \code{quiet = TRUE}.}
\item{\code{brfssdata_duplicate_label_note}}{\code{labels = TRUE} kept CDC's
numeric codes for variables whose format gives several codes
the same label, which a factor would merge into one level.}
\item{\code{brfssdata_bundled_fallback_note}}{A metadata lookup was
served from the snapshot bundled with the package (frozen at
release) because nothing newer was cached and no download was
possible.}
}
}

\keyword{internal}
