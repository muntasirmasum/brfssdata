% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{brfssdata-conditions}
\alias{brfssdata-conditions}
\title{Conditions signaled by brfssdata}
\description{
Every error, warning, and message this package signals carries a
class, so \code{tryCatch()} and \code{withCallingHandlers()} can react to
exactly the situation they mean to and nothing else, e.g.
\verb{tryCatch(read_brfss(2023), brfssdata_download_error = \\(e) NULL)} or
\code{suppressWarnings(..., classes = "brfssdata_break_warning")}.
}
\section{Errors}{

\describe{
\item{\code{brfssdata_bad_years_arg}}{\code{years} is not a vector of whole
survey years.}
\item{\code{brfssdata_bad_year}}{A requested year is not among the
published releases.}
\item{\code{brfssdata_no_data}}{The data manifest could not be read or
lists no published years.}
\item{\code{brfssdata_bad_vars_arg}}{\code{vars} is not a character vector
of variable names.}
\item{\code{brfssdata_bad_var}}{A requested variable does not exist in
the requested years.}
\item{\code{brfssdata_bad_states_arg}}{\code{states} is not a vector of
FIPS codes, postal abbreviations, or jurisdiction names.}
\item{\code{brfssdata_bad_state}}{A value in \code{states} matches no BRFSS
jurisdiction; see \link{brfss_states}.}
\item{\code{brfssdata_bad_pattern}}{\code{pattern} in \code{\link[=brfss_vars]{brfss_vars()}} is not a
valid regular expression.}
\item{\code{brfssdata_bad_weight}}{\code{weight} in \code{\link[=brfss_design]{brfss_design()}} is
malformed, or the column is absent from a requested year.}
\item{\code{brfssdata_bad_labels_arg}}{\code{labels} is something other than
\code{TRUE}, \code{FALSE}, or \code{"both"}.}
\item{\code{brfssdata_bad_na_arg}}{\code{na} is not \code{TRUE} or \code{FALSE}.}
\item{\code{brfssdata_bad_option}}{\code{options(brfssdata.lonely_psu)} is
not a single string.}
\item{\code{brfssdata_bad_design_var}}{A design variable (era weight,
\verb{_STSTR}, \verb{_PSU}) is absent or carries missing values, so no
valid design can be built.}
\item{\code{brfssdata_no_eligible_rows}}{No rows are left to build a
survey design: a \code{states} filter, or the domain of a
user-supplied \code{weight}, emptied the frame. \code{\link[=read_brfss]{read_brfss()}} still
returns the zero-row tibble, which is a usable answer; a
zero-row survey design is not constructible.}
\item{\code{brfssdata_break_error}}{The requested years span the 2011
redesign and \code{allow_break = TRUE} was not set.}
\item{\code{brfssdata_not_cached}}{\code{download = FALSE} was set and the
needed file is not in the cache.}
\item{\code{brfssdata_download_error}}{A download failed. Also the
parent class of \code{brfssdata_checksum_error}, so one handler
catches both.}
\item{\code{brfssdata_checksum_error}}{A downloaded file did not match
the manifest's sha256 after a retry; nothing was cached.}
\item{\code{brfssdata_corrupt_cache}}{A cached file is unreadable
(typically a corrupted download from before verification); the
message names the file and the \code{\link[=brfss_cache_clear]{brfss_cache_clear()}} remedy.}
\item{\code{brfssdata_type_conflict}}{A requested column is stored as
text in some requested years' files and as a number in others,
so combining them would silently corrupt values; usually stale
cached files mixed with current releases, with the
\code{\link[=brfss_cache_clear]{brfss_cache_clear()}} remedy named.}
}
}

\section{Warnings}{

\describe{
\item{\code{brfssdata_break_warning}}{Pooling across the 2011 redesign
with \code{allow_break = TRUE}.}
\item{\code{brfssdata_intermediate_weight_warning}}{\code{weight} in
\code{\link[=brfss_design]{brfss_design()}} names an intermediate stage of CDC's weighting
pipeline (e.g. \verb{_LLCPWT2}, the truncated pre-raking design
weight), not a final analysis weight.}
\item{\code{brfssdata_pooled_states_warning}}{Pooled years differ in
state participation, so totals mix coverage. Participation is
counted over the rows a user-supplied \code{weight} covers, the
population the design actually estimates, not over the whole
file.}
\item{\code{brfssdata_label_drift_warning}}{Label wording for a
variable changed meaning (not just formatting) across the
requested years, so it kept CDC's numeric codes instead of
converting to a factor; read the years separately if each
year's own wording is wanted.}
\item{\code{brfssdata_state_coverage_warning}}{A jurisdiction requested
via \code{states} is absent from a requested year's file, so
estimates for that year cover the remaining states only.}
}
}

\section{Messages}{

\describe{
\item{\code{brfssdata_cache_note}}{Cache lifecycle notes: directory
created, files removed by \code{\link[=brfss_cache_clear]{brfss_cache_clear()}}, a
size-mismatched file or stale catalog re-downloaded, or the
\code{\link[=brfss_download]{brfss_download()}} summary.}
\item{\code{brfssdata_download_note}}{A survey year is being downloaded
(once, then cached).}
\item{\code{brfssdata_manifest_note}}{The manifest or a catalog could
not be refreshed; a cached or bundled copy was used.}
\item{\code{brfssdata_lonely_psu_note}}{The once-per-session note that
\code{survey.lonely.psu} was set to \code{"adjust"}.}
\item{\code{brfssdata_unverified_note}}{An asset was downloaded without
checksum verification (the available manifest carries no hash
for it).}
\item{\code{brfssdata_na_note}}{\code{na = TRUE} set missing-type codes to
\code{NA}; the counts and the \code{\link[=brfss_missing_codes]{brfss_missing_codes()}} audit trail.}
\item{\code{brfssdata_na_coverage_note}}{\code{na = TRUE} was requested for
years the value-label catalog does not cover (before 1998) or
covers only partially (1998), so codes there passed through
unchanged.}
\item{\code{brfssdata_weight_subset_note}}{A user-supplied \code{weight} in
\code{\link[=brfss_design]{brfss_design()}} is missing on some rows (a module weight covers
only its module's records); those rows were dropped, per CDC's
module-analysis guidance.}
\item{\code{brfssdata_empty_result}}{A metadata lookup
(\code{\link[=brfss_labels]{brfss_labels()}}, \code{\link[=brfss_crosswalk]{brfss_crosswalk()}}, \code{\link[=brfss_year_info]{brfss_year_info()}})
matched nothing.}
\item{\code{brfssdata_full_load_note}}{\code{\link[=read_brfss]{read_brfss()}} is loading every
column because \code{vars} was not given; \code{\link[=brfss_design]{brfss_design()}} passes
\code{vars} through and inherits it.}
\item{\code{brfssdata_rename_note}}{A requested variable is empty in
years a sibling generation from the rename crosswalk covers;
see \code{\link[=brfss_crosswalk]{brfss_crosswalk()}}.}
\item{\code{brfssdata_bundled_fallback_note}}{A metadata lookup was
served from the snapshot bundled with the package (frozen at
release) because nothing newer was cached and no download was
possible.}
}
}

\keyword{internal}
