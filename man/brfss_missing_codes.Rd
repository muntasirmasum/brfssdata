% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_codes.R
\name{brfss_missing_codes}
\alias{brfss_missing_codes}
\title{Codes CDC uses for missing-type answers}
\usage{
brfss_missing_codes(vars = NULL, years = NULL, download = TRUE, quiet = TRUE)
}
\arguments{
\item{vars}{Optional character vector restricting to those variables,
matched case-insensitively by exact name. (Contrast \code{\link[=brfss_vars]{brfss_vars()}},
whose \code{pattern} is a regular expression searched over names \emph{and}
label text: this function looks names up, that one searches.)}

\item{years}{Optional integer vector restricting to those years.}

\item{download}{If \code{FALSE}, only a cached catalog is used, and a
missing catalog raises an error instead of being downloaded.}

\item{quiet}{If \code{TRUE}, suppress download progress output.}
}
\value{
A tibble with columns \code{year}, \code{variable}, \code{code}, and
\code{label}, one row per code the missing-value rules match. Labels
cover 1998 on, so earlier years never appear.
}
\description{
Returns the rows of the value-label catalog whose label marks a
missing-type answer: don't know / not sure, refused, or a
not-asked/missing placeholder. These are exactly the codes that
\code{na = TRUE} in \code{\link[=read_brfss]{read_brfss()}} and \code{\link[=brfss_design]{brfss_design()}} sets to \code{NA}, so
this function is the audit trail for that behavior, and the join
table for recoding by hand.

Matching is deliberately conservative. A label counts as missing when
every part of it (split on \code{/}, \verb{,}, and the word "or") is a known
missing-answer phrase, or when the only parts beyond those phrases
start with the word "missing" and at least one part names the answer
itself (don't know / not sure / refused), the shape of CDC's
calculated-variable buckets such as "Don't know, refused or missing
values" on \verb{_FRTLT1A}. The abbreviations CDC's 1998 to 2001 format
libraries use ("UNK/REF", "UNK", "REF", "UNKNOWN") count as those
phrases, as do the bare "N/A" and "N/A,REF" placeholders from the
same years. A short audited allowlist covers CDC's
"component question" wordings on the \code{RACE2} family. Substantive
answers that merely contain one of the words, such as "Doctor refused
when asked" or a bare "Missing Fruit Responses" exclusion flag, never
match. Code 88/888 ("None") is an answer of zero, not missing, and is
never matched; recode it to 0 yourself before averaging a count
variable such as \code{PHYSHLTH}.
}
\details{
This function says what \code{na = TRUE} \emph{would} clear. For what a
particular read did clear, \code{read_brfss(na = TRUE)} leaves the count
on the tibble it returns, as a \code{brfss_na_recode} attribute: one row
per variable, year, and code, with the number of values set to \code{NA}.
It is there under \code{quiet = TRUE} too, when nothing is printed, so a
missingness audit needs no second read of the raw year.
\code{attr(dat, "brfss_na_recode")} reads it. Most dplyr verbs carry it
along (\code{filter()}, \code{mutate()}, \code{select()} and their kin restore
attributes they do not recognize), but \code{summarise()} drops it, as
does anything that rebuilds the tibble from scratch, so read it off
the object \code{read_brfss()} returned rather than out of a pipeline.
}
\examples{
brfss_missing_codes("GENHLTH", years = 2023, download = FALSE)
}
\seealso{
\code{\link[=brfss_labels]{brfss_labels()}} for the full catalog.
}
