% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brfss_crosswalk.R
\name{brfss_crosswalk}
\alias{brfss_crosswalk}
\title{Rename crosswalk: which variables are generations of one measure}
\usage{
brfss_crosswalk(vars = NULL, years = NULL, download = TRUE, quiet = TRUE)
}
\arguments{
\item{vars}{Optional character vector of variable names, matched
case-insensitively by exact name like in \code{\link[=brfss_labels]{brfss_labels()}}. A match
on \emph{any} member of a family returns the whole family -- that is
the point of the lookup.}

\item{years}{Optional integer vector restricting the \code{year} rows.
The family membership shown is unaffected; only rows are filtered.}

\item{download}{If \code{FALSE}, only a cached catalog is used, and a
missing catalog raises an error instead of being downloaded.}

\item{quiet}{If \code{TRUE}, suppress download progress output.}
}
\value{
A tibble with columns \code{concept} (family identifier),
\code{variable}, \code{year}, \code{generation} (1, 2, ... in order of first
appearance), \code{status}, \code{comparable}, and \code{note}, one row per
variable-year. A lookup that matches nothing returns zero rows
with a \code{brfssdata_empty_result} message.
}
\description{
CDC renames a variable when its definition or its questionnaire
context changes, usually by bumping a trailing digit: \verb{_DRNKWK1}
becomes \verb{_DRNKWK2} becomes \verb{_DRNKWK3}. A multi-year analysis that
requests only one of those names silently loses the other years.
This function returns the crosswalk that accompanies the data
releases: variables grouped into concept families, one row per
variable and year, so the whole family is visible at once.

Families are proposed mechanically (same stem, non-overlapping year
ranges, similar label wording) and reviewed by hand against CDC's
codebooks over time. \code{status} records how far that review has gone
for each family: \code{"verified"} means a person checked it,
\code{"candidate"} means the rules proposed it and review is pending, so
treat a candidate family as a strong hint, not a fact. For verified
pairs, \code{comparable} records the outcome: \code{TRUE} when the definitions
match the previous generation, \code{FALSE} when they do not (with \code{note}
saying what changed); it stays \code{NA} while unreviewed. A rename is
\emph{never} a promise of comparability -- CDC renamed the variable for a
reason -- so combining generations is always your decision;
\code{\link[=read_brfss]{read_brfss()}} points here (a \code{brfssdata_rename_note} message) when
a requested variable is empty in years a sibling generation covers.
}
\examples{
# The whole family, from any member's name. download = FALSE reads
# the cached copy, or the snapshot bundled with the package, so this
# runs offline.
brfss_crosswalk("_DRNKWK1", download = FALSE)
}
\seealso{
\code{\link[=brfss_vars]{brfss_vars()}} to search variables; \code{\link[=brfss_codebook]{brfss_codebook()}} for
a per-variable summary that includes the family.
}
