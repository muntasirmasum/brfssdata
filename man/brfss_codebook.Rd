% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brfss_codebook.R
\name{brfss_codebook}
\alias{brfss_codebook}
\title{Codebook card: everything the catalogs know about a variable}
\usage{
brfss_codebook(vars, years = NULL, download = TRUE, quiet = TRUE)
}
\arguments{
\item{vars}{Character vector of variable names, matched
case-insensitively by exact name (required -- for browsing the
whole catalog use \code{\link[=brfss_vars]{brfss_vars()}}).}

\item{years}{Optional integer vector: restrict the value-label and
availability detail to those years.}

\item{download}{If \code{FALSE}, only a cached catalog is used, and a
missing catalog raises an error instead of being downloaded.}

\item{quiet}{If \code{TRUE}, suppress download progress output.}
}
\value{
A tibble of class \code{brfss_codebook} with columns \code{variable},
\code{label} (most recent wording), \code{years} (compact range string),
\code{values} (list-column: \code{year}, \code{code}, \code{label}, \code{complete},
\code{missing} per row), \code{missing_codes} (list-column, the \code{missing}
subset), \code{concept}, and \code{related} (list-column of sibling
generations from the crosswalk).
}
\description{
One row per requested variable, joining the three metadata catalogs:
the variable catalog (label wording and year availability), the
value-label catalog (codes and their meanings, with the missing-type
codes flagged), and the rename crosswalk (the variable's concept
family, if it belongs to one). It answers "what is this variable" in
one call; use \code{\link[=brfss_vars]{brfss_vars()}} to \emph{find} variables first.

Printing renders a card per variable. The returned object is still a
regular tibble; the \code{values} and \code{missing_codes} columns are
list-columns of tibbles, \code{related} a list-column of sibling
variable names.
}
\details{
The card documents codes and labels only. It carries no units, no
scale factor, and no valid range, so a calculated variable CDC
stores scaled (\verb{_BMI5} and \verb{_DRNKWK2} carry two implied decimals)
looks no different here from an unscaled one, and a range format
lists its special codes without the ordinary values around them.
Read magnitudes against CDC's codebook for the year, whose address
is \code{brfss_year_info()$codebook_url}.
}
\examples{
brfss_codebook("GENHLTH", years = 2023, download = FALSE)
}
\seealso{
\code{\link[=brfss_vars]{brfss_vars()}}, \code{\link[=brfss_labels]{brfss_labels()}}, \code{\link[=brfss_missing_codes]{brfss_missing_codes()}},
\code{\link[=brfss_crosswalk]{brfss_crosswalk()}}.
}
