% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brfss_labels.R
\name{brfss_labels}
\alias{brfss_labels}
\title{Value labels for BRFSS variables}
\usage{
brfss_labels(vars = NULL, years = NULL, download = TRUE, quiet = TRUE)
}
\arguments{
\item{vars}{Optional character vector restricting to those variables,
matched case-insensitively by exact name. (Contrast \code{\link[=brfss_vars]{brfss_vars()}},
whose \code{pattern} is a regular expression searched over names \emph{and}
label text: this function looks names up, that one searches.)}

\item{years}{Optional integer vector restricting to those years.}

\item{download}{If \code{FALSE}, only a cached catalog is used, and a
missing catalog raises an error instead of being downloaded.}

\item{quiet}{If \code{TRUE}, suppress download progress output.}
}
\value{
A tibble with columns \code{year}, \code{variable}, \code{code}, \code{label},
and \code{complete}. A lookup that matches nothing returns zero rows and
says so with a \code{brfssdata_empty_result} message (regardless of
\code{quiet}, which governs download output only). When only some
requested variables match, the matching rows are returned and a
\code{brfssdata_partial_match_note} message names the ones with no
entries, also regardless of \code{quiet}.
}
\description{
Returns the value-label catalog that accompanies the data releases:
one row per year, variable, and numeric code, with the label text from
CDC's SAS format libraries. Labels cover 1998 onward; CDC does not
distribute usable format libraries for earlier years.

The \code{complete} column marks variables whose format for that year is a
pure code-to-label map (no numeric ranges such as \code{1-30} days). Only
those variables are eligible for automatic factor conversion via
\code{read_brfss(labels = TRUE)}; for the rest, the catalog still documents
the special codes (typically 77/88/99) so you can recode by hand.
}
\examples{
# download = FALSE reads the cached catalog, or the snapshot bundled
# with the package, so this runs offline.
brfss_labels("GENHLTH", years = 2023, download = FALSE)
}
